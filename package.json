{
  "name": "based-demo",
  "version": "1.0.0",
  "description": "Demo for Based JWT flow template",
  "license": "UNLICENSED",
  "main": "dist/server/index.js",
  "scripts": {
    "start": "port=3030 && node dist/server/index.js",
    "dev": "aristotle watch -t src/index.tsx -p 3030",
    "build": "aristotle build -t src/index.tsx -d dist",
    "postbuild": "cd ./dist && npm install --production"
  },
  "dependencies": {
    "@based/client": "^3.0.1",
    "@based/pretty-date": "^1.0.4",
    "@based/react": "^3.0.2",
    "@based/ui": "^4.0.0-alpha.6"
  },
  "devDependencies": {
    "@based/cli": "^2.9.0",
    "@based/server": "^3.0.1",
    "@saulx/aristotle": "^4.3.1"
  }
}
